# LP制作指示

## 目的・基本方針

指定されたFigmaデザインを基準として、見た目を忠実に再現するLPを制作する。

- デザインを独自に解釈・改善・省略しない。
- レイアウト、余白、色、フォント、画像、装飾を元デザインに合わせる。
- 推測で描き直すのではなく、Figmaの設計データと書き出し素材を使用する。
- デザインや素材が不足している箇所は明示し、仮置きと確定済みの内容を区別する。

## 制作に必要な情報

- Figma URL：https://www.figma.com/design/otnVHe5ZRTTez8xxxHCo0A/Follow-LP?node-id=2-3&t=OdDQp0g6MGYpO3lX-1
- 対象フレーム／ノードID：`2:3`（ルートGroup。PC/SP全アートボードが1つのGroupに横並びで入っている。子は `2:4` BACK画像 / `2:488` BACKカラー / `2:787` DESIGN の3層）
  - 2026-09-07 にアートボード単位のフレーム 9 枚へ分割済み（PC-Menu `14:2` / PC-1 `14:6` / PC-2 `14:10` / PC-3 `14:14` / SP-Menu `18:2` / SP-1 `18:6` / SP-2 `18:9` / SP-3 `18:13` / SP-4 `18:17`）。座標・検証結果は `docs/figma-artboards.md`。分割前の原本はページ `4:2` にバックアップ。
  - テキストノードは0件。全文字がアウトライン化された `vector`（8,197個）で、レイヤー名も「Vector」「Group」のみ。Figmaから文字列・フォント・行間は取得できない。
- PC版の基準幅：1920px（LP本体は3アートボード、高さ 8826 + 8635 + 10107 = 27,568px。4枚目 1920x1000 はハンバーガーメニュー展開図）
- スマートフォン版デザイン：同一Figmaファイル内。基準幅 750px（LP本体4アートボード 9377 + 9353 + 8557 + 6018px。5枚目 750x1400 はメニュー展開図）
- 使用フォント・提供素材：
  - `design/FOLLOW_LP_PC.pdf` / `design/FOLLOW_LP_SP.pdf`（Illustrator直接書き出し、フォント22種埋め込み）。文字はこのPDFが唯一の正確なソース。
  - フォントは商用が中心（筑紫AオールドMinPr6N、DNP秀英角ゴシック銀、ゴシックMB101、UD角ゴLarge、ロダン、Futura PT、DIN 2014、DIN Pro、Hiragino Sans、Noto Sans CJK JP、AntroVectra ほか）。Web配信ライセンスは未確認。
  - 写真・書き出し素材：`assets/`（一覧と元ファイル名は `ASSETS.md`）。原稿 .ai / .psd は `design/`。
- CTAのリンク先・フォームの動作：**リンク先なし・フォームなし**（2026-09-07 決定）。CTAは当たり判定の座標のみ保持し、「リンク未設定」であることをプレビュー上で明示する。`href="#"` や `javascript:void(0)` のダミーは使わない。遷移先確定後に `a` 要素へ差し替える。
- 動的要素（静止画積み上げでは成立しない箇所）：FAQアコーディオン、ハンバーガーメニュー。**この 2 つは必須**（2026-09-07 ユーザー指示）。Instructor は WordPress 連携なしのため静的画像。
- デザイン上の赤いボックス（「WordPressで編集」`2:4721`、「アコーディオン機能でお願いします。」`2:4735`）は注記なので実装しない（2026-09-07 ユーザー指示）。
- ページタイトル・説明文（Claude Code が仮決定、2026-09-07。変更はユーザー指示で）：
  - title: `follow｜豊洲駅徒歩1分 マシンピラティス・パーソナルセッション専門スタジオ`
  - description: `豊洲駅1c出口から徒歩1分。スタジオ専門のfollowで、ひとりひとりをきちんと見る本格パーソナルセッション。はじめての方限定の体験セッション（45分）￥8,800。`
- 使用する技術：**Next.js**（App Router、TypeScript、`output: 'export'` の静的書き出し、`images.unoptimized: true`）と、必要に応じて Python（切り出し・合成・差分）。**WordPress 連携は不要**（2026-09-07 ユーザー指示）。

## データ取得の役割分担（2026-09-07 決定）

- 見た目（ピクセル）の正解：Illustrator直接書き出しの PDF（`design/`）をセクション単位で画像化する。Figmaの画像書き出しは使わない。
- 座標・ノードID・スクリーンショット：Figma MCP（`get_metadata` / `get_screenshot`）。取得結果は `docs/` に保存して再取得しない。9 フレーム分の `get_metadata` は `docs/figma-meta/<frame>.xml` に保存済み（2026-09-07）。要約は `python3 tools/figma_meta_outline.py`。
- 文字列・フォント・字間：PDF から取得する。Figmaにはテキストノードが無いため使えない。`pdftotext -bbox-layout` の結果を `docs/pdf-text/<page>.xml`（行一覧 `*.lines.txt`、`python3 tools/pdf_text_lines.py`）、フォント・色は `pdftohtml -xml` の結果を `docs/pdf-text/fonts/` に保存済み。
- セクション境界・CTA 矩形・FAQ 行・メニュー項目の台帳：`docs/sections.md`。PDF 切り出しの定義は `docs/slices.json`、実行は `python3 tools/pdf_slice.py docs/slices.json --scale=1 --out=build/img`（出力 `build/img/{pc,sp}/`）。
- Codex は素材監査・独立検証、Claude Code は実装を担当する（`docs/codex-review-2026-09-07.md` 参照）。Codex の呼び出しは `codex exec --skip-git-repo-check -s <sandbox> -o <out.md> - < prompt.md`（モデルは設定既定の `gpt-6-astra` のみ）。ブラウザ検証は `sh tools/codex/verify.sh <unit>`。sandbox は `danger-full-access` が必要（`workspace-write` では listen EPERM・Chromium 起動拒否）。パイプで `head` に繋ぐと SIGPIPE で途中終了するので、出力はファイルに落とす。

## 再現度の基準：画素差分 0.000%（2026-09-07 ユーザー指示）

「似ている」ではなく「一致している」を目標にする。画素差分＝元デザイン画像とブラウザ撮影画像を重ねたときに異なる画素の割合。**0.000%＝1 画素も違わない。**

- 考え方：「AI に再現させる」のをやめ、「デザインが持っている本物の画素を、正しい位置にそのまま置く」。AI に絵を描かせない。ブラウザに描かせるのは配置だけ。
- 正解画像（**2026-09-07 改定、ユーザー承認**）：`design/*.pdf` を **`pdftocairo -png -r 72`**（1pt=1px）で描画した可逆 PNG。PC は幅 1920px（CSS 幅 1920 に 1x 表示）、SP は幅 750px（CSS 幅 375 に 2x 表示）。レンダラを `pdftoppm`（Splash）から cairo に変えた理由は、Splash の stroke adjust で細線幅（ハンバーガー 3 本線）が 2px/3px に不揃いになるため。PC を 144dpi・2x にする案は一度実装したが、見た目の差が小さく容量が 3.5 倍になるためユーザー指示で 1x に戻した（2026-09-07 夜）。根拠は `docs/verify/01-header-fv/render-analysis.md`（Codex 独立分析 `codex-render-analysis.md`）。Figma スクリーンショットは正解に使わない（PDF とレンダラ差がある）。
- 比較条件：Chromium（Playwright）。PC は viewport 幅 1920・DPR 1、SP は viewport 幅 375・DPR 2（device px 750）。`pixelmatch` threshold 0。差分画素 0 個で合格。条件と数値は `docs/verify/<unit>/` に記録する。PC を DPR 2 の画面で見ると 1x 画像が拡大補間されるため 0.000% の対象外（仕様として記録）。
- 0.000% はこの基準幅・基準 DPR でのみ保証する。他の幅は同じ画像を比例拡縮して表示する（仕様として記録）。
- 見える画素はすべて PDF 切り出し PNG から来る。ブラウザにフォントを描かせない。CSS で色・線・角丸・影を描かない。`next/image` の最適化は使わない。切り出し境界と配置はすべて整数 px。**SP の切り出しはページ累積の device y が全境界で偶数**（CSS 375px・DPR 2 で .5px を出さない。`tools/pdf_slice.py` が `dpr` で検査。2026-09-07 単位 03 で確定）。
- 動的要素も画像の組み合わせで成立させる：ハンバーガーはメニュー展開図の画像を重ねる。CTA は透明な当たり判定のみ。Instructor は静的画像。
  - ハンバーガー（単位 02、2026-09-07 実装）：展開図のヘッダー部は通常ヘッダーと異なる（PC はタグライン無し・× 置換）ため、展開図を **y=0 から全面**で `position: fixed` に重ねる。基準 viewport は PC 1920×1000 / SP 375×700@2x。**それより高い viewport では展開図の下（デザイン未定義領域）を展開図左端の色 `#d1c1a2` で埋める**（CSS で色を描く唯一の例外、0.000% 対象外。ユーザー承認待ち）。低い viewport はメニュー層内だけ縦スクロール。
- **例外：FAQ の質問・回答は実テキスト**（2026-09-07 ユーザー決定）。FAQ の Q/A 本文は 0.000% の対象外とし、フォント・色・行送りを PDF に合わせて近似する（差分は数値で記録）。FAQ 見出し帯や周辺の画像部分は 0.000% を維持する。FAQ 各行の高さはデザイン値に固定し、後続の Access 以降の位置がずれないようにする。
- ヘッダー・フッター黒帯 CTA は**静的**（固定・追従なし。2026-09-07 決定）。
- ブレークポイント **768px**（767px 以下は SP デザインを 375 基準で比例拡縮、768px 以上は PC デザインを 1920 基準で比例縮小）。**1920px 超は中央寄せ**で左右は背景色（2026-09-07 決定）。
- 計画の詳細：`thoughts/plans/2026-09-07_pixel-exact-implementation-plan.md`。

## 進行ルール（2026-09-07 ユーザー指示）

1 レビュー単位（`thoughts/plans/2026-09-07_pixel-exact-implementation-plan.md` §5 の表）ごとに、次を完了してからユーザーに提出する。

1. 実装（PC・SP 同時）。
2. `node tools/verify/run.mjs --unit <unit>` で元デザインと比較。差分があれば修正して再比較。PC/SP とも差分 0 になるまで繰り返す。
3. **Codex（`gpt-6-astra`、`codex exec` を Bash から実行）に独立検証させる。** ブラウザで実装と正解画像の両方を開いて比較し、数値の再現・差分画像の確認・キーボード操作・開閉後のずれ・横スクロールを報告させる。結果は `docs/verify/<unit>/codex.md`。
4. 差分数値・条件・確認方法・残課題を添えて提出し、**承認を得てから次の単位へ進む。**

- ユーザーに戻るのは「単位の提出」と「ユーザーの割り込み」の 2 場面に限る。それ以外は自律的に進める。
- **セッションのトークン使用量が 80% に達したら**（残量表示が総量の 20% 以下になったら）、単位の途中でも `/create-handoff` を実行し、続けて `/resume-handoff` で再開する。
- ゴール：全単位を 1 つずつ完璧に仕上げ、都度承認を得て、すべての単位をやり切る。

## 実装方式

### セクション画像を積み上げる方式

画像中心で、見た目の一致を最優先するLPに採用する。文字の編集性・検索性に制約があることを踏まえて選択する。

- デザインをセクション単位の画像として縦に配置する。
- CTAなどの操作箇所に、位置と大きさを合わせたリンクを重ねる。
- リンクには内容が分かるアクセシブルな名前と、キーボード操作時のフォーカス表示を設定する。
- 画面幅が変わっても画像とリンクの位置がずれないようにする。

## Figmaデータの取得

1. 対象フレーム全体の基準画像を、デザインの原寸で取得する。
2. 設計データから座標、サイズ、色、文字列、フォント、行間、字間などを取得する。
3. パスが必要な場合は、ノード取得時に `geometry=paths` を指定する。
4. 取得済みデータと素材を保存し、不要な再取得を避ける。
5. 複数ノードは、可能な範囲で `ids` にまとめて取得する。
6. API制限が発生した場合は、応答の再試行指示に従う。必要なら手動書き出し素材を使用する。

## 素材・文字の実装ルール

- 部品の識別にはノードIDを使用し、同名レイヤーの取り違えを防ぐ。
- 原寸画像から切り抜く場合は、フレーム基準の座標と書き出し倍率を反映する。
- 影、透過、重なりを確認し、切り抜きによる欠けや背景の混入を防ぐ。
- 複雑なイラストや装飾を、似た画像や推測したCSSで置き換えない。
- HTML文字はフォント、ウェイト、サイズ、行間、字間、色、配置を合わせる。
- `U+2028` を含む改行と連続スペースを適切に処理し、デザイン上の改行を維持する。
- 本 LP では 0.000% を優先し、FAQ の質問・回答以外の文字は PDF 切り出し画像で表示する（2026-09-07 決定）。画像内の文字は `alt` で提供する。

## 縦長LPの画像取得

- 画像書き出しで縮小や欠落が起きる場合は、PDF書き出しを検討する。
- PDFを使用する場合は、必要な表示サイズに合わせて画像化する。
- 文字の脱落、ぼけ、画像欠落がないか元デザインと比較する。
- 問題が出た場合は、倍率の変更やセクション分割で再検証する。
- 小さい画像の拡大だけで、画質が改善したものとして扱わない。

## レスポンシブ対応

- PC・スマートフォンは同一URLで提供し、メディアクエリで対応する。
- ブレークポイントはデザインに合わせて決める。
- 各画面幅で横スクロール、文字の欠け、要素の重なり、リンク位置のずれを確認する。

## 検証・修正

1. 基準画像と同じ表示幅でブラウザのスクリーンショットを取得する。
2. フォントと画像の読み込み完了を確認して撮影する。
3. 元デザインとの重ね合わせ・画像差分でずれを確認する。
4. レイアウト、素材、フォント、余白、色の順に大きな差を修正する。
5. 修正後に再撮影し、比較を繰り返す。
6. 画像差分は **0.000%（差分画素 0 個）** を合格条件とし、使用した指標と比較条件を記録する。未測定の数値や根拠のない完全一致を報告しない。
7. PC・スマートフォンの両方で、CTA、リンク、フォームの動作を確認する。

## ページ情報・納品

- ページタイトルと説明文を設定する。
- 画像には用途に応じた代替テキストを設定する。
- SEO目的の非表示キーワードは埋め込まない。
- 納品時に、実装方式、確認方法、検証結果、残っている差異や未設定事項を簡潔に報告する。